# Example env for server. Copy to `.env` (not committed) and fill values for deployment.
PORT=5000
MONGO_URI=your_mongo_connection_string_here
JWT_SECRET=your_jwt_secret_here
# OAuth callbacks (optional) - set these to the full callback URLs your OAuth providers expect
# GOOGLE_CALLBACK_URL=https://unsplash-image-search-app.onrender.com/api/auth/google/callback
# GITHUB_CALLBACK_URL=https://unsplash-image-search-app.onrender.com/api/auth/github/callback
# FACEBOOK_CALLBACK_URL=https://unsplash-image-search-app.onrender.com/api/auth/facebook/callback
